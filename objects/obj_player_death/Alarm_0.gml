game_set_speed(60, gamespeed_fps)

// calls the trasition script with effect and the room to be restarted
//SlideTransition(TRANS_MODE.GOTO, room)

// calls in the death fade and teleports the player to the last active teleport
global.deathTransition = true